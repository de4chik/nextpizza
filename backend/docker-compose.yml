version: '3.8'
services:
  db:
    image: postgres
    container_name: db
    environment:
      POSTGRES_USER: evgenijevtehov
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"